import Layout from '../../layout/Layout';
import Flow from './ReactFlowTest';

const RoadmapPage = () => {
  return (
    <Layout>
      로드맵 메인
      <Flow />
    </Layout>
  );
};

export default RoadmapPage;
