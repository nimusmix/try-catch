import Layout from '../../layout/Layout';
import { Button } from '../../components';

const LandingPage = () => {
  return (
    <Layout>
      <Button />
    </Layout>
  );
};
export default LandingPage;
