import { Slideshow } from '../../components';
import Layout from '../../layout/Layout';

const HomePage = () => {
  return (
    <Layout>
      <Slideshow />
    </Layout>
  );
};

export default HomePage;
